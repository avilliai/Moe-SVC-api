#配置你的
riri:             #触发名，可自定义
  speaker: riri       #以模型配置文件即config里面的spk项为准
  model: logs/G_riri_220.pth   #模型对应根目录的相对路径
  config: configs/config.json  #配置文件相对路径
丁真:             #触发名，可自定义
  speaker: ding       #以模型配置文件即config里面的spk项为准
  model: logs/litangking.pth   #模型对应根目录的相对路径
  config: configs/dingzhenconfig.json  #配置文件相对路径